# Repository agent instructions

Read and obey the canonical `ORESoftware/.github` lowercase `agents.md` before
editing. Keep the runnable implementation dependency-free and standards-based;
proposal-only JavaScript syntax belongs in documentation, not the test path.
